hazelcast:
   network:
      port:
         port: ${hazelcast.port}
         auto-increment: false
      join:
         tcp-ip:
            enabled: false
         multicast:
            enabled: false