hazelcast:
   config:
      network:
         port: ${hazelcast.port}
         port-auto-increment: false
         join:
            tcp-ip:
               enabled: true
            multicast:
               enabled: false