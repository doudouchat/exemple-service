hazelcast:
   config:
      network:
         port: ${hazelcast.port}
         port-auto-increment: false
         join:
            tcp-ip:
               enabled: false
            multicast:
               enabled: false