<ruleset comparisonMethod="maven">
	<ignoreVersions>
		<ignoreVersion type="regex">(?i).*atlassian(?:-?\d+)?</ignoreVersion>
		<ignoreVersion type="regex">(?i).*redhat(?:-?\d+)?</ignoreVersion>
		<ignoreVersion type="regex">(?i).*(alpha|beta|RC|M)(?:-?\d+)?</ignoreVersion>
	</ignoreVersions>
	<rules>
		<rule groupId="com.hazelcast" comparisonMethod="maven">
			<ignoreVersions>
				<ignoreVersion type="regex">4.*(?:-?\d+)</ignoreVersion>
			</ignoreVersions>
		</rule>
	</rules>
</ruleset>